#include<stdio.h>
#include <string.h>
int random(s,i){
    int ran,length;
    ran=s*s;
    if(len(ran)<(len(s)+len(s)){

       }
    char characters;
    characters=ran;


}
int main()
{

    int i,rn;
    int array[50];
    long int seed;
    printf("Enter the seed number:\n");
    scanf("%ld",&seed);
    printf("Enter the number of random numbers to generate:\n");
    scanf("%d",&rn);
    for(i=0;i<rn;i++){

            array[i]= random(seed,i);

    }


}
